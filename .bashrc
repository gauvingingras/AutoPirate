# docker-compose
alias stackup='docker-compose up -d --remove-orphans'
alias stacklogs='docker-compose logs -f'
alias stackdown='docker-compose down'
alias stackrestart='docker-compose restart'

# tree
alias treeprint='tree -d -L 2'
